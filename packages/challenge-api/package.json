{
  "name": "challenge-api",
  "version": "0.0.1",
  "private": false,
  "author": "Yuri Martins",
  "description": "app api",
  "main": "build/index.js",
  "scripts": {
    "app": "json-server --watch --port 5000 ./src/db.json",
    "build": "babel src --out-dir ./build --source-maps -D"
  },
  "babel": {
    "presets": [
      "module:challenge-babel"
    ]
  },
  "devDependencies": {
    "json-server": "^0.16.1",
    "nodemon": "^2.0.4"
  }
}
